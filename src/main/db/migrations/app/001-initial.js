/**
 * Initial migration - schema is applied via app-schema.sql, this is a placeholder.
 * @param {import('better-sqlite3').Database} db
 */
module.exports = function migrate(db) {
    // Schema already applied by database.js applySchema
};
